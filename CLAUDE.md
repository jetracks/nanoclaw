# Legacy Compatibility Note

NanoClaw now uses `AGENTS.md` as the primary repo guide and `groups/*/AGENTS.md` as the primary memory files.

If a tool reads `CLAUDE.md`, treat this file as a compatibility shim and follow `AGENTS.md` instead.
